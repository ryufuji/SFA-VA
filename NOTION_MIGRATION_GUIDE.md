# Notionからのデータ移行ガイド

## 概要

このガイドでは、NotionデータベースからSFAシステムへのデータ移行方法を説明します。

## 移行方法の選択

### 方法A: CSV手動インポート（推奨）
- **難易度**: 低
- **所要時間**: 30分〜2時間
- **推奨ケース**: データ量が中規模以下（各テーブル1000件未満）

### 方法B: Notion API自動連携
- **難易度**: 高
- **所要時間**: 実装に数時間
- **推奨ケース**: 定期的な同期が必要、データ量が大規模

---

## 方法A: CSV手動インポート（詳細手順）

### ステップ1: Notionデータベースの準備

#### 1.1 データベース構造の確認

Notionで管理している以下のデータベースを確認してください：

**必須データベース：**
- 顧客/リード情報
- 案件情報
- 契約情報
- メンバー情報

**オプション：**
- 月次明細情報
- 入金情報

#### 1.2 SFAシステムとのフィールド対応表

**リード（顧客情報）のマッピング例：**

| Notionフィールド | SFAフィールド | 必須 | 変換ルール |
|-----------------|--------------|------|-----------|
| 会社名 / Company Name | company_name | ○ | そのまま |
| 担当者名 / Contact | contact_person | - | そのまま |
| メールアドレス / Email | email | - | そのまま |
| 電話番号 / Phone | phone | - | そのまま |
| ステータス / Status | status | ○ | active/archived に変換 |

**案件情報のマッピング例：**

| Notionフィールド | SFAフィールド | 必須 | 変換ルール |
|-----------------|--------------|------|-----------|
| 案件名 / Project Name | project_name | ○ | そのまま |
| 顧客 / Client (リレーション) | lead_id | ○ | 会社名からID検索 |
| ステータス / Status | status | ○ | active/won/lost/archived に変換 |
| 営業担当 / Sales Rep | sales_rep_id | - | メンバー名からID検索 |

**メンバー情報のマッピング例：**

| Notionフィールド | SFAフィールド | 必須 | 変換ルール |
|-----------------|--------------|------|-----------|
| 名前 / Name | name | ○ | そのまま |
| メールアドレス / Email | email | ○ | そのまま |
| 単価 / Unit Price | default_unit_price | ○ | 数値に変換 |
| 役職 / Position | position | - | そのまま |
| メモ / Notes | memo | - | そのまま |
| ステータス / Status | status | ○ | active/inactive に変換 |

**契約情報のマッピング例：**

| Notionフィールド | SFAフィールド | 必須 | 変換ルール |
|-----------------|--------------|------|-----------|
| 契約名 / Contract Name | contract_name | ○ | そのまま |
| 案件 / Project (リレーション) | project_id | ○ | 案件名からID検索 |
| 開始日 / Start Date | contract_start_date | ○ | YYYY-MM-DD に変換 |
| 終了日 / End Date | contract_end_date | ○ | YYYY-MM-DD に変換 |
| 契約金額 / Amount | contract_amount | ○ | 数値に変換 |
| 契約形態 / Type | contract_type | - | 準委任/請負/その他 |
| 支払形態 / Payment Type | payment_type | - | 毎月支払/初回全額支払 |
| ステータス / Status | status | ○ | draft/active/completed/terminated |

---

### ステップ2: Notionからのデータエクスポート

#### 2.1 各データベースのエクスポート

1. Notionで各データベースページを開く
2. 右上の「**...**」（その他のオプション）をクリック
3. 「**エクスポート**」を選択
4. エクスポート形式：
   - 「**Markdown & CSV**」を選択（推奨）
   - または「**CSV**」のみでも可
5. 「**すべてをエクスポート**」を選択
6. 「**エクスポート**」ボタンをクリック
7. ZIPファイルがダウンロードされる
8. ZIPファイルを解凍

#### 2.2 エクスポートされたCSVファイルの確認

解凍後、以下のようなファイル構造になります：

```
Export-XXXXXX/
├── 顧客データベース [hash].csv
├── 案件データベース [hash].csv
├── 契約データベース [hash].csv
└── メンバー [hash].csv
```

各CSVファイルを開いて内容を確認してください。

---

### ステップ3: CSVデータの整形

NotionのCSVは以下の特徴があるため、整形が必要です：

#### 3.1 Notion特有のフォーマット

**日付フィールド：**
- Notion形式: `2026-01-15T00:00:00.000Z`
- SFA形式: `2026-01-15`
- 変換方法: ExcelやGoogleスプレッドシートで日付部分のみを抽出

**ステータスフィールド：**
- Notion形式: 日本語ラベル（進行中、完了、中断など）
- SFA形式: 英語キーワード（active, won, lost, archived）
- 変換ルール:
  ```
  進行中 / 商談中 / アクティブ → active
  受注 / 成約 / 完了 → won
  失注 / キャンセル → lost
  アーカイブ / 終了 → archived
  ```

**複数選択フィールド（タグなど）：**
- Notion形式: `タグ1, タグ2, タグ3`
- SFA対応: 現時点ではタグ機能未実装のため、メモ欄に記載

**リレーションフィールド：**
- Notion形式: 関連ページ名（例：`株式会社サンプル`）
- SFA対応: 2段階インポートで対応（後述）

#### 3.2 整形用のテンプレート

以下のスプレッドシート関数を使用して整形できます：

**日付変換（Googleスプレッドシート）：**
```
=LEFT(A2, 10)
```

**ステータス変換：**
```
=IF(A2="進行中","active",IF(A2="受注","won",IF(A2="失注","lost","archived")))
```

**数値変換（カンマ区切りの削除）：**
```
=VALUE(SUBSTITUTE(A2,",",""))
```

---

### ステップ4: SFAシステムへのインポート

#### 4.1 インポート順序（重要）

リレーション（関連データ）があるため、以下の順序でインポートしてください：

```
1. メンバー情報 ← 他のデータから参照される
2. リード（顧客）情報 ← 案件から参照される
3. 案件情報 ← 契約から参照される
4. 契約情報
5. 月次明細情報（オプション）
```

#### 4.2 CSVインポート手順（各エンティティ共通）

1. SFAシステムにログイン（管理者アカウント）
2. 管理メニューから「**データインポート**」を選択
3. インポート対象を選択（例：リード）
4. 「**テンプレートをダウンロード**」ボタンでCSV形式を確認
5. 整形したCSVファイルを選択
6. 「**プレビュー**」ボタンで内容を確認
7. オプション設定：
   - ☑ **重複データをスキップ**（推奨：チェック）
   - ☑ **テスト実行**（初回は必ずチェック）
8. 「**インポート実行**」ボタンをクリック
9. 結果を確認：
   - 成功件数
   - エラー件数
   - エラー詳細
10. エラーがある場合はCSVを修正して再実行
11. テスト実行で問題なければ、「テスト実行」のチェックを外して本番実行

#### 4.3 リレーションデータの対応方法

**案件インポート時の注意：**
- `lead_id`（顧客ID）は、インポート前にリード一覧から該当する顧客のIDを確認
- または、CSVに顧客名を記載し、インポート時に自動マッチング機能を使用

**契約インポート時の注意：**
- `project_id`（案件ID）は、インポート前に案件一覧から該当する案件のIDを確認

**マッチング用のCSVフォーマット例：**

```csv
project_name,company_name,sales_rep_email,status
システム開発案件A,株式会社サンプル,yamada@example.com,won
```

システムが自動的に以下の変換を行います：
- `company_name` → `lead_id`（会社名でリード検索）
- `sales_rep_email` → `sales_rep_id`（メールアドレスでメンバー検索）

---

### ステップ5: データ整合性の確認

インポート完了後、以下を確認してください：

#### 5.1 件数チェック

```
□ リード件数: Notion ____ 件 → SFA ____ 件
□ 案件件数: Notion ____ 件 → SFA ____ 件
□ 契約件数: Notion ____ 件 → SFA ____ 件
□ メンバー件数: Notion ____ 件 → SFA ____ 件
```

#### 5.2 サンプルデータの目視確認

- 各エンティティから数件をランダムに選び、Notionと比較
- 特に以下のフィールドを重点的に確認：
  - 日付フィールド
  - 金額フィールド
  - ステータス
  - リレーション（関連データ）

#### 5.3 ダッシュボードの確認

- ダッシュボードの集計値が妥当か確認
- グラフが正しく表示されるか確認

---

## 方法B: Notion API自動連携（高度な方法）

### 前提条件

- Notion APIの知識
- プログラミングスキル（JavaScript/TypeScript）
- Notionインテグレーションの作成権限

### 実装概要

```typescript
// Notion API からデータ取得
const notion = new Client({ auth: NOTION_API_KEY });

// データベースクエリ
const response = await notion.databases.query({
  database_id: NOTION_DATABASE_ID,
});

// データ変換とSFAシステムへの登録
for (const page of response.results) {
  const data = extractProperties(page);
  await importToSFA(data);
}
```

### 必要な手順

1. Notionインテグレーションの作成
2. データベースへのアクセス権限付与
3. データ変換ロジックの実装
4. エラーハンドリングとログ記録
5. 定期実行設定（必要に応じて）

**注意**: この方法は技術的に複雑なため、必要に応じて別途実装支援を提供します。

---

## トラブルシューティング

### よくある問題と解決方法

#### Q1: CSVファイルの文字化けが発生する

**原因**: 文字エンコーディングの不一致

**解決方法**:
1. CSVをExcelで開く
2. 「名前を付けて保存」→ 「CSV UTF-8 (カンマ区切り)」を選択
3. または、テキストエディタで開いてUTF-8で保存

#### Q2: 日付が正しくインポートされない

**原因**: Notionの日付形式がそのまま使用されている

**解決方法**:
- エクセルで `=LEFT(A2, 10)` を使用して日付部分のみ抽出
- または、手動で `YYYY-MM-DD` 形式に整形

#### Q3: リレーションデータが見つからない

**原因**: 関連データのインポート順序が間違っている

**解決方法**:
1. 必ず依存元のデータから先にインポート
   - メンバー → リード → 案件 → 契約
2. IDマッチング用のCSVフォーマットを使用

#### Q4: 重複データが登録されてしまった

**原因**: 「重複データをスキップ」オプションが無効だった

**解決方法**:
1. データベースから重複データを削除
2. メールアドレスや会社名でソートして重複を特定
3. 再インポート時は必ず「重複データをスキップ」をチェック

#### Q5: インポート実行時にエラーが多発する

**原因**: 必須フィールドが空、またはフォーマットが不正

**解決方法**:
1. エラーメッセージを確認
2. CSVの該当行を修正
3. 「テスト実行」モードで再度確認
4. エラーがなくなるまで繰り返す

---

## サポートとお問い合わせ

### データ移行でお困りの場合

- エラーログのスクリーンショットを共有
- Notionのデータベース構造（スクリーンショット可）を共有
- 具体的なエラー内容を説明

これらの情報があれば、より具体的なサポートが可能です。

---

## チェックリスト

移行作業の進捗管理にご利用ください：

### 事前準備
- [ ] Notionデータベース構造の確認
- [ ] SFAシステムとのフィールド対応表作成
- [ ] テスト環境での動作確認

### データエクスポート
- [ ] メンバー情報をNotionからエクスポート
- [ ] リード情報をNotionからエクスポート
- [ ] 案件情報をNotionからエクスポート
- [ ] 契約情報をNotionからエクスポート
- [ ] ZIPファイルを解凍してCSV取得

### データ整形
- [ ] 日付フィールドをYYYY-MM-DD形式に変換
- [ ] ステータスを英語キーワードに変換
- [ ] 数値フィールドのフォーマット統一
- [ ] リレーション用のマッチングキー追加

### インポート実行
- [ ] メンバー情報インポート（テスト実行）
- [ ] メンバー情報インポート（本番実行）
- [ ] リード情報インポート（テスト実行）
- [ ] リード情報インポート（本番実行）
- [ ] 案件情報インポート（テスト実行）
- [ ] 案件情報インポート（本番実行）
- [ ] 契約情報インポート（テスト実行）
- [ ] 契約情報インポート（本番実行）

### 確認作業
- [ ] 各エンティティの件数確認
- [ ] サンプルデータの目視確認
- [ ] ダッシュボード表示確認
- [ ] リレーションデータの確認

### 移行完了
- [ ] ユーザーへの通知
- [ ] 操作説明の実施
- [ ] Notion側のアーカイブ

---

## 次のステップ

移行が完了したら、以下のドキュメントもご参照ください：

- ユーザー操作マニュアル（作成予定）
- 管理者ガイド（作成予定）
- バックアップとリストア手順（作成予定）
